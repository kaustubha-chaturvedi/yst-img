name: Auto Plugin Release

on:
  workflow_dispatch:
  push:
    branches: 
      - main
      - master
    tags:
      - "v*"

jobs:
  build:
    name: Build (${{ matrix.os }} / ${{ matrix.goarch }})
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            runner: ubuntu-latest
            goarch: amd64

          - os: darwin
            runner: macos-15
            goarch: amd64

          - os: darwin
            runner: macos-latest
            goarch: arm64

          - os: windows
            runner: windows-latest
            goarch: amd64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: Install Linux dependencies
        if: matrix.os == 'linux'
        run: |
          sudo apt update
          sudo apt install -y libavif-dev pkg-config

      - name: Install macOS dependencies
        if: matrix.os == 'darwin'
        run: |
          if [[ "$(uname -m)" == "arm64" ]]; then
            brew install libavif pkg-config
            echo "PKG_CONFIG_PATH=/opt/homebrew/lib/pkgconfig" >> $GITHUB_ENV
          else
            export HOMEBREW_NO_AUTO_UPDATE=1
            export HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK=1
            brew install libavif pkg-config
            echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig" >> $GITHUB_ENV
          fi
      
      - name: Build libavif for Windows (static)
        if: matrix.os == 'windows'
        shell: powershell
        run: |
          # Install vcpkg
          git clone https://github.com/microsoft/vcpkg.git
          .\vcpkg\bootstrap-vcpkg.bat

          # Install static libavif (includes aom, dav1d, etc.)
          .\vcpkg\vcpkg install libavif:x64-windows-static

          # Create embed directory
          New-Item -ItemType Directory -Path embed/windows/include -Force
          New-Item -ItemType Directory -Path embed/windows/lib -Force

          # Copy headers
          Copy-Item "vcpkg/installed/x64-windows-static/include/avif" -Recurse -Destination "embed/windows/include/"

          # Copy static lib
          Copy-Item "vcpkg/installed/x64-windows-static/lib/avif.lib" -Destination "embed/windows/lib/"

      - name: Build plugin
        shell: bash
        run: |
          mkdir -p dist
          BIN="yst-img-${{ matrix.os }}-${{ matrix.goarch }}"
          if [ "${{ matrix.os }}" = "windows" ]; then
            BIN="$BIN.exe"
          fi

          CGO_ENABLED=1 \
          GOOS=${{ matrix.os }} \
          GOARCH=${{ matrix.goarch }} \
          go build -tags avif -o dist/$BIN .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: yst-img-${{ matrix.os }}-${{ matrix.goarch }}
          path: dist/*
      
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true

      - name: List files
        run: ls -lh release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

